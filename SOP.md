# TradePilot 數據同步操作手冊 (SOP)

本手冊定義了系統在 **Phase A** 階段的正式資料同步流程。為確保數據指標計算的準確性，系統採用「Google Sheets 計算、JSON 檔案中轉、前端渲染呈現」的單向手動同步架構。

---

## 核心系統邊界

在開始操作前，請務必釐清以下系統權責邊界：

| 項目 | Google Sheets (Apps Script) 端 | 前端 UI 網頁端 |
| :--- | :--- | :--- |
| **主要定位** | 資料儲存庫與**運算引擎核心** | 互動式資料展示與**個人筆記 (UI Ext)** |
| **資料來源** | 實體試算表 (`stock_db`, `settings`, `meta`) | 前端 `localStorage` 快取 |
| **CSV 解析** | **支援**。利用試算表 API 與系統邏輯計算均線與買賣訊號 | **不支援**。前端無運算引擎，僅提供日誌記錄 |
| **個人筆記** | 不儲存個股的 `tags` 與 `notes` | **儲存並保留**。物理隔離於 `ui_extensions` |

---

## 標準操作同步流程 (SOP)

### 步驟 1：於 Google Sheets 匯入 CSV 並計算
1. 開啟您的 **TradePilot Google 試算表**。
2. 點擊自訂選單中之 **「手動匯入 CSV」** 執行 Apps Script。
3. 選取個股系統匯出的原始 CSV 檔案（Big5 / UTF-8）。
4. **等待運算完成**：Apps Script 會自動調用 `ZoneEngine` 進行區間判定、均線計算、量能篩選，並重新寫入 `stock_db` 工作表。

### 步驟 2：從 Apps Script 匯出 JSON 備份
1. 在試算表介面中，點擊自訂選單之 **「導出備份 JSON」**。
2. 系統會根據 `BackupPayload`（v1.0.0 契約規格）打包包含 `stock_db`、`settings`、`meta`、`import_log` 四個工作表的數據。
3. 將產出的 JSON 檔案儲存至您的本機電腦。

### 步驟 3：在前端 UI 匯入 JSON 數據
1. 開啟 TradePilot 前端網頁。
2. 點擊右上角控制列的 **「資料同步與備份」** 按鈕打開抽屜。
3. 切換至 **「JSON 同步與備份」** 頁籤。
4. 在 **「還原系統資料庫 (JSON 數據同步)」** 區塊中，點擊 **「選擇 JSON 檔案匯入還原 (數據同步)」**。
5. 選擇剛才從 Google Sheets 匯出的 JSON 檔案。
6. 看到「資料庫還原成功」提示即完成同步。

### 步驟 4：資料完整性檢查
1. **檢查日誌**：切換至「系統日誌」頁籤，應出現一筆狀態為「成功」的日誌：`已從 JSON 還原整個資料庫｜來源系統=...`。
2. **檢查交易日**：查看前端主畫面的系統資訊欄位，確認「交易日」已更新為本次 CSV 的最新交易日期。
3. **檢查個股數值**：驗證列表中的「現-成交」、「漲跌幅」、「買入/賣出上緣下緣」是否與 Google Sheets 的數據一致。
4. **確認個人筆記與標籤**：確認個股的 Tags 與 Notes 依然存在（系統還原時會自動保留您的前端自訂筆記，不會覆蓋）。

---

## 關鍵注意事項與誤解防範

> [!CAUTION]
> ### 🚨 最容易誤解的四大操作誤區
>
> 1. **誤區一：嘗試直接在前端網頁上傳 CSV**
>    - *真相*：前端的「CSV 同步」分頁僅用於模擬日誌測試。直接在此匯入 CSV **完全不會** 更新網頁上的股票資料。請務必在 Google Sheets 執行 CSV 匯入。
> 
> 2. **誤區二：以為 Google Sheets 更新後，前端網頁會自動同步**
>    - *真相*：目前 Phase A 兩端是**完全隔離**的。Sheets 上的任何新增、修改或 CSV 匯入，**必須** 經由「下載 JSON → 匯入 JSON」才會反映至網頁前端。
> 
> 3. **誤區三：擔心匯入 JSON 備份會洗掉我在前端寫的個股 Notes / Tags**
>    - *真相*：筆記與標籤已在前端進行「物理隔離」儲存。還原備份僅會覆蓋價格與公式計算結果等核心欄位，**個人筆記與標籤會 100% 完整保留**。
> 
> 4. **誤區四：在 Google Sheets 修改參數設定 (Settings) 後直接看網頁**
>    - *真相*：參數設定也屬於備份的一部分，修改後同樣需要重新下載並匯入 JSON。

---

## 未來升級規劃 (Phase B)

當前 Phase A 手動流程穩定後，未來預計將進行 **Phase B (GAS Web App API)** 升級：
- 屆時 Google Sheets 將部署為 Web App 並開放 API 端點。
- 前端將可以直接透過 API 進行即時讀取與回寫，免除手動匯入 JSON 之步驟。
- 在 Phase B 之前，請務必以此手動 SOP 作為唯一正式資料同步路徑。
